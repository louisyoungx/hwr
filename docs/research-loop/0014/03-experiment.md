# R0014 冻结实验

## 状态

尚未冻结。必须先完成独立提案与独立筛选。

冻结时至少记录：

- 入选假设和唯一主变量；
- 基线、候选与源码提交；
- 负责人、分支或 worktree、文件所有权；
- 完整命令、配置、seed 和输入哈希；
- 样本单位、主要指标、守护指标；
- wall time、RSS、磁盘和加速器预算；
- 接受、拒绝、无效、超预算和证据不足条件；
- provenance、无泄露、独立性和历史轮次冻结守护。
